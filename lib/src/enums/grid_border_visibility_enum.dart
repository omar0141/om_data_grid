/// Defines which borders should be visible in the grid.
enum OmGridBorderVisibility {
  /// No borders are visible.
  none,

  /// Only horizontal borders are visible.
  horizontal,

  /// Only vertical borders are visible.
  vertical,

  /// Both horizontal and vertical borders are visible.
  both,
}
