/// Defines the scrolling / loading mode for the mobile data grid.
enum OmMobileScrollMode {
  /// Shows paginated pages with navigation controls at the bottom.
  pagination,

  /// Automatically loads more items when the user scrolls to the bottom.
  infiniteScroll,
}
