/// Defines the layout type for the mobile data grid view.
enum OmMobileViewType {
  /// Single-column full-width cards.
  list,

  /// Two-column card grid layout.
  grid,

  /// Compact single-line list tiles.
  compact,
}
